package test;

extern void println();
extern void println(int i);

typename i32 = int;
typename size_t = ulong;

void func(byte a, i32 b) {
	const int i = 5 + a * 3;
}

const size_t version = 4;

uint testf() {
	uint u = 17;
	u += 5;
	return u;
}

struct pos {
	int x;
	int y;
};

void sum(pos a, pos b) {
	pos c = {a.x + b.x, a.y + b.y};
}

//const _Allocator a_local = _Allocator.create();

void main() {

/*
	pos* p = new(a_local) pos;

	delete(a_local) p;
*/


	pos p = {4,7};
	p.x = 16;

	sum(p,{10,10});


}
