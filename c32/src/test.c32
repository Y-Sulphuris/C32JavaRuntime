package test;

const auto nullptr = (void*)0;

byte test() {
	return 5b;
}

void test2(byte b) {
	std::println(b);
	return;
}

void test3(int i2, int* l, int i6) {
	int b3 = i2;
	return;
}

void main() {
	const int len = 64;
	int* p_i = std::malloc(len);
    p_i[0] = 50;
	int index = 0;
	//NEXT:
	if(index == len) return;
	std::println(p_i[index]);
	std::Core::sleep(100);
	std::println(p_i[++index]);
	std::Core::sleep(100);
	std::println(p_i[++index]);
	std::Core::sleep(100);
	std::println(p_i[++index]);
	//goto NEXT;
	return;
}

