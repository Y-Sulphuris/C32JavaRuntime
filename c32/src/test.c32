package test;

const auto nullptr = (void*)0;

void main() {
	size_t size = 5ul;
	int* mem = std.malloc(8);
	*mem = 5;
	mem[1] = 40;
	std.println(*mem);
	std.println(*(mem+1));
	std.println(mem[0]);
	std.println(mem[1]);
}

