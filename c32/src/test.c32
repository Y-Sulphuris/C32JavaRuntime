package test;

const auto nullptr = (void*)0;

byte test() {
	return 5b;
}

void test2(byte b) {
	std::println(b);
	return;
}

void test3(int i2, int* l, int i6) {
	int b3 = i2;
	return;
}

import std.calloc;

void main() {
	int len = 64;
	int* p_i = calloc(len, sizeof(int));
	std::memset(p_i,255ub,len * sizeof(decltype(*p_i)));
    p_i[0] = 50;
	int index = 0;
    std::println(p_i[index]);
	while(index < len) {
		std::print(++index);
		auto space = '\t';
		std::print(space);
        std::println((void*)p_i[index]);
        std::Core::sleep(100);
	}
	std::free(p_i);

	return;
}

