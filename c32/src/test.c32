package test;

const auto nullptr = (void*)0;

byte test() {
	return 5b;
}

void test2(byte b) {
	println(b);
	return;
}

void test3(int i2, int* l, int i6) {
	int b3 = i2;
	return;
}

import std.println, std.print;

void main() {
	import std.Core.*;

	int len = 16;
	int* p_i = new_int_array(len), new_int_array(size_t len) {
        return std::calloc(len, sizeof(int));
    };
	std::memset(p_i, 0xFEub, len * sizeof(decltype(*p_i)));
    p_i[0] = 50;
	int index = -1;
	while(index < len-1) {
		print(++index);
		auto space = '\t';
		print(space);
        println((void*)p_i[index]);
        sleep(100);
	}
	len = 32;
	p_i = std::realloc(p_i, len);

	while(index < len-1) {
    	print(++index);
    	auto space = '\t';
    	print(space);
        println((void*)p_i[index]);
        sleep(100);
    }

	std::free(p_i);

	return;
}

