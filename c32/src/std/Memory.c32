package std;

typename octet = ubyte;

extern void* malloc(size_t size);
extern void* calloc(size_t size, size_t num);
extern void* realloc(void* mem, size_t size);
extern void free(void* mem);

extern void* memcpy(void* src, void* dst, size_t size);
extern void* memset(void* dst, octet val, size_t size);
extern int memcmp(void* buf1, void* buf2, size_t size);
